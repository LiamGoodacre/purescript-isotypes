{
  "private": true,
  "scripts": {
    "build": "pulp build --censor-lib --strict",
    "test": "pulp test",
    "dev": "pscid --test",
    "build:json": "pulp build --no-psa --json-errors",
    "build:docs": "pulp docs"
  },
  "devDependencies": {
    "bower": "^1.7.9",
    "pulp": "^9.0.1",
    "purescript-psa": "^0.3.9"
  }
}
